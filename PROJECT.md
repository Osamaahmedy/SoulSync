# SoulSync — منصة الرفاهية النفسية والدعم المجتمعي

## 🎯 فكرة المشروع
**SoulSync** هي منصة ويب تهدف إلى تعزيز الصحة النفسية من خلال توفير مساحة آمنة للمستخدمين للتعبير عن أفكارهم ومشاعرهم وامتنانهم، مع إمكانية تتبع حالتهم المزاجية عبر الزمن من خلال رسوم بيانية تحليلية.

---

## 🛠️ التقنيات المستخدمة

| التقنية | الاستخدام |
|---------|-----------|
| **Laravel 13** | إطار العمل الأساسي (Backend) |
| **MySQL** | قاعدة البيانات |
| **Blade** | محرك القوالب (Frontend) |
| **Chart.js** | الرسوم البيانية التحليلية |
| **Laravel Breeze** | نظام المصادقة (Authentication) |
| **Gmail SMTP** | إرسال البريد الإلكتروني (استعادة كلمة السر) |
| **Vanilla CSS** | التصميم المخصص (Premium Design) |
| **Tailwind CSS CDN** | تصميم صفحات المصادقة |

---

## 📂 هيكل المشروع

```
lumina/
├── app/
│   ├── Http/Controllers/
│   │   ├── SparkController.php      ← إدارة المشاركات (CRUD)
│   │   ├── DashboardController.php  ← لوحة التحكم والإحصائيات
│   │   └── ProfileController.php    ← إدارة الملف الشخصي
│   └── Models/
│       ├── User.php                 ← نموذج المستخدم
│       ├── Spark.php                ← نموذج المشاركة (الشرارة)
│       ├── Category.php             ← نموذج التصنيف
│       └── Reaction.php             ← نموذج الإعجاب
├── database/
│   ├── migrations/                  ← ملفات إنشاء الجداول
│   └── seeders/                     ← بيانات تجريبية
├── resources/views/
│   ├── welcome.blade.php            ← الصفحة الرئيسية (Community Feed)
│   ├── dashboard.blade.php          ← لوحة التحكم الشخصية
│   ├── auth/                        ← صفحات الدخول والتسجيل
│   ├── profile/                     ← صفحة الملف الشخصي
│   └── layouts/                     ← القوالب الأساسية
├── public/css/app.css               ← ملف التصميم الرئيسي
└── routes/web.php                   ← تعريف المسارات
```

---

## ✨ مميزات المشروع

### 1. نظام المصادقة (Authentication)
- تسجيل حساب جديد
- تسجيل الدخول / الخروج
- نسيت كلمة السر (إرسال رابط عبر البريد الإلكتروني)
- إدارة الملف الشخصي (تعديل الاسم، الإيميل، كلمة السر)
- حذف الحساب

### 2. لوحة المجتمع (Community Feed)
- عرض جميع المشاركات (Sparks) من كل المستخدمين
- تصفية المشاركات حسب التصنيف (Personal Growth, Work, Health, Social, Learning)
- إضافة مشاركة جديدة مع تحديد التصنيف ودرجة المزاج
- نظام الإعجابات (Ignite) مع تأثيرات بصرية متحركة

### 3. لوحة التحكم الشخصية (Dashboard)
- إحصائيات: إجمالي المشاركات، متوسط المزاج، عدد الإعجابات، أيام الاستمرارية
- رسم بياني خطي: تتبع المزاج خلال آخر 7 أيام (Chart.js)
- رسم بياني دائري: توزيع المشاركات حسب التصنيفات
- جدول إدارة المشاركات مع إمكانية **التعديل** و**الحذف**
- نافذة تعديل (Modal) تفاعلية

### 4. التصميم (UI/UX)
- تصميم مظلم احترافي (Dark Theme)
- تأثيرات Glassmorphism
- متجاوب بالكامل (Responsive) لجميع أحجام الشاشات
- تأثيرات حركية (Animations) للأزرار والبطاقات
- تدرجات لونية (Gradients) مخصصة

---

## 🗄️ قاعدة البيانات

### جدول `users` (المستخدمين)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | bigint | المعرف الفريد |
| name | varchar | اسم المستخدم |
| email | varchar | البريد الإلكتروني |
| password | varchar | كلمة السر (مشفرة) |

### جدول `sparks` (المشاركات)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | bigint | المعرف الفريد |
| content | text | محتوى المشاركة |
| author | varchar | اسم الكاتب |
| color | varchar | لون البطاقة |
| user_id | bigint (FK) | معرف المستخدم |
| category_id | bigint (FK) | معرف التصنيف |
| mood_score | integer | درجة المزاج (1-10) |

### جدول `categories` (التصنيفات)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | bigint | المعرف الفريد |
| name | varchar | اسم التصنيف |
| icon | varchar | أيقونة التصنيف |
| color | varchar | لون التصنيف |

### جدول `reactions` (الإعجابات)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | bigint | المعرف الفريد |
| user_id | bigint (FK) | معرف المستخدم |
| spark_id | bigint (FK) | معرف المشاركة |

---

## 🔗 المسارات (Routes)

| Method | URL | الوصف |
|--------|-----|-------|
| GET | `/` | الصفحة الرئيسية (Community Feed) |
| POST | `/sparks` | إنشاء مشاركة جديدة |
| POST | `/sparks/{id}/toggle` | إعجاب / إلغاء إعجاب |
| GET | `/dashboard` | لوحة التحكم الشخصية |
| PUT | `/sparks/{id}` | تعديل مشاركة |
| DELETE | `/sparks/{id}` | حذف مشاركة |
| GET | `/login` | صفحة تسجيل الدخول |
| POST | `/login` | تنفيذ تسجيل الدخول |
| GET | `/register` | صفحة إنشاء حساب |
| POST | `/register` | تنفيذ إنشاء الحساب |
| GET | `/forgot-password` | طلب استعادة كلمة السر |
| GET | `/profile` | صفحة الملف الشخصي |

---

## 🚀 خطوات التشغيل

```bash
# 1. تثبيت المكتبات
composer install

# 2. إنشاء قاعدة البيانات
mysql -u root -p -e "CREATE DATABASE lumina_db;"

# 3. نسخ ملف البيئة وتعديل إعدادات قاعدة البيانات
cp .env.example .env
# عدّل DB_DATABASE, DB_USERNAME, DB_PASSWORD

# 4. توليد مفتاح التطبيق
php artisan key:generate

# 5. تنفيذ التهجير مع البيانات التجريبية
php artisan migrate --seed

# 6. تشغيل السيرفر
php artisan serve
```

ثم افتح المتصفح على: **http://127.0.0.1:8000**

---

## 👤 المطور
**Osama** — مشروع تخرج 2026

---

## 📄 الرخصة
هذا المشروع مبني لأغراض تعليمية (مشروع تخرج).
